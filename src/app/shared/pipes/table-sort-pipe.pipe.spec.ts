import { TableSortPipePipe } from './table-sort-pipe.pipe';

describe('TableSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TableSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
